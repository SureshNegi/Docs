Virtual Keyword:
ToString() in base class Object is virtual.Why? so that any child class can overide it.
    public class Object
    {
       
        // Summary:
        //     Returns a string that represents the current object.
        //
        // Returns:
        //     A string that represents the current object.
        public virtual string ToString();
       }
Object o=new C1();o.Tostring();  //It will print whatever we have written in the overriten method
C1{override ToString(){......}   }       
       
